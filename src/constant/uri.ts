const enum uri {
  auth = "/auth",
  register = "/register",
  email = "/email",
  image = "/image",
  my = "/my-info",
  myDiaryList = "/diary-list",
  profileImage = "/profile-image",
  intro = "/intro",
  match = "/match",
  chronology = "/diary/chronology",
}

export default uri;
