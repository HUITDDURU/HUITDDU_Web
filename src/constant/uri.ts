const enum uri {
  auth = "/auth",
  register = "/register",
  email = "/email",
  image = "/image",
  my = "/my-info",
  myDiaryList = "/diary-list",
  profileImage = "/profile-image",
  intro = "/intro",
  match = "/match",
  chronology = "/diary/chronology",
  diary = "/diary",
  list = "/list",
  code = "/code",
  mate = "/mate",
}

export default uri;
